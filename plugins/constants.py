from typing import Any, Dict

CONFIG_TYPE = Dict[str, Any]

DUPLICATED_NAME = "Name plugin is duplicated"
PLUGIN_NOT_FOUND = "Plugin not found"
INVALID_REQUEST_METHOD = "Invalid request method"

INVALID_REQUIRED_BODY_SCHEMA = "Invalid body schema (doesn't have required field)"
INVALID_TYPE_BODY_SCHEMA = "Invalid body schema (doesn't have type field)"
